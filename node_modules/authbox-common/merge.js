'use strict';
function merge() {
  var x = {};

  Array.prototype.slice.call(arguments).forEach(function(a) {
    for (var k in a) {
      x[k] = a[k];
    }
  });
  return x;
}

module.exports = merge;
