'use strict';
function keyMirror(obj) {
  var nextObj = {};
  for (var k in obj) {
    if (!obj.hasOwnProperty(k)) {
      continue;
    }
    nextObj[k] = k;
  }
  return nextObj;
}

module.exports = keyMirror;
