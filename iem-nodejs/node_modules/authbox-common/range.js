'use strict';
var emptyFunction = require('./emptyFunction');

function range(n, valueFactory) {
  var array = [];
  if (!valueFactory) {
    valueFactory = emptyFunction.thatReturnsArgument;
  }
  for (var i = 0; i < n; i++) {
    array.push(valueFactory(i));
  }
  return array;
}

module.exports = range;
