'use strict';
var invariant = require('../invariant');
describe('invariant', function() {
  it('prints correctly', function() {
    expect(function() {
      invariant(true, 'you won\'t see me');
      invariant(false, 'what the %s', 'hell');
    }).toThrow('what the hell');
  });
});
